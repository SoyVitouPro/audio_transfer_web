Place your branding assets here.

- logo.png: used in the header (rounded) and as primary favicon if present
- favicon.png: used as favicon (falls back to logo.svg if missing)

Provided fallback: logo.svg (simple gradient circle).
